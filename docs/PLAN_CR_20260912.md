# PLAN — 3 CR đăng ký/cập nhật US + tách view H1 (2026-09-12)

> Trạng thái: **CHỜ [TT] DUYỆT PLAN** trước khi code. Khảo sát hiện trạng đã xong; context đã chốt qua hỏi-đáp.

## 0. Quyết định đã chốt (từ [TT])
| # | Quyết định | Chọn |
|---|---|---|
| CR1-a | Mức rút gọn đăng ký | **Tối giản triệt để**: đăng ký chỉ còn WF + US + Action Plan theo tháng; bỏ toàn bộ B1 nghiệp vụ (trừ WF/US/Team) + B2 prompt + B3 demo/impact + B4 hướng dẫn → dời sang "Cập nhật US" |
| CR1-b | Stage → Plan theo giai đoạn | **4 ô kế hoạch T9–T12** (bỏ Stage khỏi đăng ký; lưu 4 cột mới) |
| CR2 | "Cập nhật US" thay "Cập nhật tuần" | **Thay hẳn thành editor US** (sửa prompt/luồng AI + demo + số liệu + action plan tháng); bỏ khái niệm "báo cáo tuần" |
| CR3 | View US H1 | **Menu "US H1" riêng, chỉ đọc**; đọc từ tab `Data H1` (cùng spreadsheet AIUS, cột giống MASTER_DATA) |
| Stage | Phạm vi retire Stage | **Chỉ gỡ khỏi đăng ký + cập nhật**; GIỮ cột `Current_Stage` + pill hiển thị ở dashboard/thư viện (backward-compat) |

## 0b. Điểm nhỏ em sẽ default (anh phủ nhận nếu khác)
- US mới tạo qua đăng ký tối giản → `Current_Stage` mặc định **`S1 - Idea`** (âm thầm) để dashboard/thư viện/pill không vỡ.
- Đăng ký vẫn **auto-fill + khóa** Owner (từ session) và **auto-chọn Team** (đổi được) — cần cho chấm điểm/nhóm.
- CR2: **giữ** timeline nhưng đổi nhãn "Báo cáo tuần" → **"Lịch sử cập nhật"** (tái dùng WEEKLY_LOG làm nhật ký chỉnh sửa); **giữ** thanh % tiến độ (optional); **bỏ** toggle nâng Stage + checklist tiêu chí.
- Menu "US H1" hiển thị cho **mọi role đăng nhập** (read-only).
- 4 cột mới: `Action_Plan_M09`, `Action_Plan_M10`, `Action_Plan_M11`, `Action_Plan_M12` (thêm CUỐI HEADERS → `ensureSheetColumns_` self-heal, không lệch cột cũ).

---

## 1. CR1 — Đăng ký tối giản (WF + US + Action Plan T9–T12)

### FE
- **`assets/js/constants.js`**
  - `FIELDS`: thêm `ACTION_PLAN_M09..M12`.
  - `STEPS`: rút còn **1 bước** "Đăng ký nhanh": `Workflow`, `UseCase_Name`, `Team`, (Owner ẩn/auto) + 4 textarea `Action_Plan_M09..M12`. **Xóa** Step 2/3/4 khỏi đăng ký. Bỏ `Current_Stage`, `Pain_Point`, `Current_Process`, `Current_Time_Min`, `Current_Problem`, `User_Type`, `Expected_Goals` khỏi bước đăng ký.
  - `FIELD_CONFIG`: thêm 4 field action plan (textarea, group mới `plan`, helper "Việc dự kiến làm trong tháng").
  - `GROUP_CONFIG`: +`plan: { label: 'Kế hoạch hành động theo tháng (T9–T12)' }`.
- **`assets/js/wizard.js`**: totalSteps=1 → ẩn indicators/progress/nút Trước-Sau, hiện thẳng nút Gửi. WF→US cascade giữ nguyên.
- **`assets/js/validation.js`**: `step1`/`all` → bỏ required `Pain_Point`/`Current_Process`/`Flow_Description`; **required = `Workflow` + `UseCase_Name`** (+ ≥1 ô action plan — cân nhắc).
- **`assets/js/app.js`**: `_bindDuplicateCheck` truyền `''` cho pain (không còn field); giữ `_workflowGroupOf` (lưu `Workflow_Group`); submit không đổi luồng.
- **`index.html`** (section `register`): wizard tự render từ STEPS; ẩn phần indicator khi 1 bước. Cache-bust `?v=`.

### GAS (⚠️ cần redeploy)
- **`Config.gs`**: HEADERS += 4 cột action plan (cuối bảng). `REQUIRED_FIELDS_CREATE` = bỏ `Pain_Point`,`Current_Process`,`Flow_Description`; **thêm `Workflow`**; giữ `UseCase_Name`,`Owner_Name`,`Owner_Email`,`Team`.
- **`UseCaseService.gs`** (create): nếu `Current_Stage` rỗng → set `'S1 - Idea'`. Duplicate: pain rỗng → dice theo tên (đã tự chịu vì weight, kiểm lại `checkDuplicate_`).

---

## 2. CR2 — "Cập nhật US đã đăng ký" (thay "Cập nhật tuần")

### FE
- **`index.html`**
  - Sidebar: nhãn "Cập nhật tuần" → **"Cập nhật US"** (giữ `data-view="weekly-update"` để không xáo router; chỉ đổi text + tiêu đề).
  - Section `weekly-update`: **bỏ** ô bắt buộc "Cập nhật tuần này *" (`#weeklyUpdate`) làm trọng tâm; **mở sẵn** accordion Prompt/Luồng AI (thành khối chính); thêm ô sửa **Demo** (status/link) + **4 ô Action Plan T9–T12** (prefill từ US); **bỏ** khối nâng Stage (`upgradeToggle`/checklist/`s4Fields`). Giữ % tiến độ + số liệu (active/monthly/hours/reuse) + timeline (đổi nhãn "Lịch sử cập nhật").
- **`assets/js/weekly-update.js`**: đổi tiêu đề; `_wuInit` bỏ bind upgradeToggle; `submitWeeklyUpdate` → payload thêm `Action_Plan_M09..M12` + `Demo_Status/Demo_Link`; **bỏ** nhánh milestone/stage (`New_Stage`/`Scale_Plan`). Prompt/luồng: bỏ điều kiện `_promptTouched` (giờ là editor chính — luôn gửi bản đang hiển thị sau khi prefill).

### GAS (⚠️ cần redeploy)
- **`WeeklyReportService.gs`** (route `weekly-update`): nhận + ghi `Action_Plan_M09..M12`, `Demo_Status/Demo_Link` vào MASTER; **bỏ** xử lý stage-upgrade/milestone (hoặc giữ hạ tầng nhưng FE không gửi). WEEKLY_LOG vẫn ghi 1 dòng/lần = nhật ký chỉnh sửa.

---

## 3. CR3 — View "US H1" (đọc từ tab `Data H1`, chỉ đọc)

### GAS (⚠️ cần redeploy)
- **`Config.gs`**: `SHEETS.MASTER_H1 = 'Data H1'`.
- **`Code.gs`**: route mới `h1-list` → đọc tab `Data H1` bằng mapper giống `list` (cột giống MASTER_DATA), trả mảng read-only. (Không ghi.)

### FE
- **`index.html`**: +nav "US H1" (`data-view="us-h1"`, mọi role) + `<section data-view="us-h1">` bảng read-only (tái dùng style library).
- **`assets/js/us-h1.js`** (mới): `Router.register('us-h1', …)` → gọi `h1-list`, render bảng (Mã/US/Team/Owner/Workflow/Stage pill + action plan nếu có), có ô tìm kiếm nhẹ. Cache-bust + thêm `<script>`.

---

## 4. Rủi ro & Test
- **Test Playwright (118)**: bộ test register (4 bước/nhiều field), weekly-update (text tuần + nâng stage) sẽ **fail** → phải viết lại theo form mới. Thêm test cho `us-h1`. **Đây là phần công đáng kể** — tách commit riêng, verify từng bước.
- **GAS redeploy**: gộp 1 lần sau khi xong cả 3 CR (Config + UseCaseService + WeeklyReportService + Code) — việc [TT].
- **Backward-compat**: US cũ thiếu 4 cột action plan → rỗng; `ensureSheetColumns_` tự thêm cột. Stage cũ giữ nguyên.
- **Data-boundary**: 0 PII/secret; `Data H1` là dữ liệu nội bộ.

## 5. Trình tự đề xuất (staged, mỗi bước verify)
1. **S1 — CR1** đăng ký tối giản (FE constants/wizard/validation/app + Config/UseCaseService) → viết lại test register.
2. **S2 — CR2** editor "Cập nhật US" (FE weekly-update + WeeklyReportService) → viết lại test update.
3. **S3 — CR3** view US H1 (Config/Code route + us-h1.js + index) → +test us-h1.
4. Full Playwright xanh → cross-ref hub + push → **[TT] redeploy GAS 1 lần** + hard-refresh nghiệm thu.
